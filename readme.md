Para instalar o venv no linux
```
sudo apt install python3.10-venv
```

Criando a virtual env
```
python3 venv venv
```

Ativando a virtual env
```
source venv/bin/activate
```

Instalando o flask
```
pip install Flask
```